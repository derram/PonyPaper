<?xml version="1.0" encoding="utf-8"?>
<!--
  Deprecated: the custom pony editor is built with Gradle.

    ./gradlew :custom:jar
    java -jar custom/build/libs/customponies.jar

  This Ant script is no longer maintained (its source paths pointed at the
  pre-Gradle layout). Prefer the Gradle :custom module.
-->
<project default="jar">
    <fail message="Use Gradle: ./gradlew :custom:jar (see custom/README.md and BUILDING.md)" />
</project>
